syntax = "proto3";

package chat;

service ChatService {
    
  rpc ChatStream(stream ChatMessage) returns (stream ChatMessage);
}

// The message being sent
message ChatMessage {
  string user = 1;   
  string message = 2; 
  int64 timestamp = 3; 